# caddy-auth-json
A Caddy module for HTTP authentication against JSON-returning APIs. Extracts fields via JSON Pointers into placeholders/vars to enable fine-grained access control using standard Caddy matchers or CEL expressions.
